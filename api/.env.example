# JWT secret key used for signing and verifying tokens. This should be a long, random string.
JWT_SECRET=your_jwt_secret_key_here

# Storage directory for uploaded PDF files. This directory must be outside the repository.
# Docker Compose overrides this with its persistent named volume location.
UPLOAD_DIRECTORY=/var/lib/documind/uploads
